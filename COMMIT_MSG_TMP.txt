fix(safety): make the EN caveat provable in the DOM; close verification-record gaps

Follow-ups from the second verification pass (verdict: challenged, 6 dimensions).

CODE (the one substantive gap)
- LangProvider hard-coded 'zh', so the English surface was unrenderable and the
  "zh + EN" claim could only be asserted through the dictionary - which proves a
  translation exists, not that the result page ever shows it. Added an
  `initialLang` prop (default 'zh', no behaviour change for the app) and a DOM
  test that renders a low verdict in EN and asserts 'Evidence strength',
  'not a probability of safety', 'No strong risk signal found', 'Low risk', and
  still no success class / shield-check. Red first: the EN render failed.

RECORD
- docs/upgrade-notes.md: the deviations section now records all three boundary
  deviations explicitly, including the two the verifier could not find adjudicated:
  (2) acceptance-criterion wording vs delivered behaviour for fusion - AC2's
  "agreement may narrow" is SUPERSEDED by this task's own red line that nothing may
  make a low-risk verdict look more certain, so narrowing is forbidden (a literal
  x0.85 narrowing would need re-approval against that red line); (3) the eval
  script is .ts rather than the .py named in the brief, because the engine is
  TypeScript and a Python replayer would be a re-implementation rather than a
  measurement of the real engine.
- Stale comments that still described the retracted narrowing (merge.test.ts
  header, evidence.test.ts contract block) corrected to match the assertions.
- Test counts re-pinned to the freshly measured 454 vitest / 49 pytest.

Verification: vitest 454 green, pytest 49 green, build green, eval:safety exit 0
with grading unchanged (0/3916 false-safe, 8.79% false alarms, 95.45% accuracy),
and the eval guard still fails loudly (2 failed / 11 skipped) with the dataset
absent rather than reporting a vacuous pass.
